class Bookmark extends BookmarkExplorerElement {
    constructor(url, name, icon) {

        super(name);

        this.url = url;
        this.icon = icon;
    }

    open() {

    }

    customize() {
        
    }
}