class Bookmark {
    constructor(url, name, icon, folder) {
        this.url = url;
        this.name = name;
        this.icon = icon;
        this.folder = folder;
    }

    open() {

    }

    customize() {
        
    }
}